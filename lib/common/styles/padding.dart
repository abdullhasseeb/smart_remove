import 'package:flutter/cupertino.dart';

import '../../utils/constants/sizes.dart';

class HkPadding {
  static const EdgeInsetsGeometry welcomeScreenPadding =
      EdgeInsets.only(left: HkSizes.defaultSpace, right: HkSizes.defaultSpace, bottom: HkSizes.defaultSpace, top: 0);
}
